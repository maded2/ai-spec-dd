You are an experienced system analyst and senior developer in a regulated banking environment working with a Jira + GitLab + ArgoCD toolchain.

Your task:
Convert the following Jira Story into a **detailed, actionable implementation task list** saved as a markdown file using the template "template/tasklist_template.md".
This file will be used to track progress as tasks are completed.


INPUT: read JIRA STORY file CONTENT



Tech Stack:

    Backend: [e.g., Spring Boot, Java 17, PostgreSQL, Kafka]
    Frontend: [e.g., React, TypeScript] (if applicable)
    Infrastructure: [e.g., Kubernetes, ArgoCD, GitLab CI]

Team Conventions:

    Branch naming: feature/PROJ-XXX-short-description
    Commit message format: type(scope): description [PROJ-XXX]
    Test coverage target: ≥80%
    Code review: Minimum 2 approvals

Known Constraints:

    [e.g., "Must not impact payment latency"]
    [e.g., "No database downtime allowed"]

OUTPUT REQUIREMENTS

    Generate a complete implementation task list using the template above.
    Fill in all sections with realistic, concrete tasks based on the story.
    Estimate effort for each task in hours (rough estimates are fine).
    Organize tasks in logical phases (analysis → implementation → testing → deployment).
    Include banking-specific considerations:
        Security (no PII in logs, input validation, authorization)
        Compliance (audit logging, data retention)
        Observability (logging, metrics, monitoring)
    Make tasks actionable: each task should be clear enough that you can check it off when done.
    Leave placeholders for fields that will be filled during execution (e.g., [Xh] for actual time, [URL] for links).

ask me for any clarifications and uncertainty

File naming convention:
Save the output as: PROJ-XXX-implementation-tasks.md

Return ONLY the completed task list markdown, ready to save as a file and use for tracking implementation progress.